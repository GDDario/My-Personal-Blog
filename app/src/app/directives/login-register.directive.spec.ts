import { LoginRegisterDirective } from './login-register.directive';

describe('LoginRegisterDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginRegisterDirective();
    expect(directive).toBeTruthy();
  });
});
